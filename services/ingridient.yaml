asyncapi: 2.5.0

info:
  title: Ingridient Service
  version: 0.0.1

channels:
  recipe.deleted:
    description: Inform about a recipe is deleted.
    publish:
      message:
        $ref: "../common/messages.yaml#/recipeDeleted"

  ingridient.deleted:
    description: Inform about an ingridient is deleted.
    subscribe:
      message:
        $ref: "../common/messages.yaml#/ingridientDeleted"
